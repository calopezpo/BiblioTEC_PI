document.addEventListener("DOMContentLoaded", () => {
    const randomBookButton = document.getElementById("randomBook");

    if (randomBookButton) {
        randomBookButton.addEventListener("click", () => {
            alert("La Ruleta Literaria se conectará después con la lógica del sistema.");
        });
    }
});
